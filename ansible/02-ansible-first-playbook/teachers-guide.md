# Teacher Notes

Understanding the following command:

`$ ansible-playbook -i 'localhost,' -c local playbook.yml`

`ansible-playbook` -

`-i 'localhost,'` -

`-c local` -

`playbook.yml` -



# Dev Notes
